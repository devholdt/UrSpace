{
	"name": "urspace",
	"version": "1.0.0",
	"description": "",
	"main": "dist/js/index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "sass src/scss:dist/css && cp -r src/js dist/js && cp src/images dist/images",
		"start": "live-server",
		"dev": "npm-run-all --parallel watch start",
		"docs": "jsdoc -c ./conf.json -r"
	},
	"keywords": [],
	"author": "devholdt<mathias@herholdt.dev>",
	"license": "ISC",
	"devDependencies": {
		"live-server": "^1.2.2",
		"npm-run-all": "^4.1.5",
		"sass": "^1.66.1"
	},
	"dependencies": {
		"bootstrap": "^5.3.1"
	}
}
